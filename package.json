{
  "dependencies": {
    "package-lock": "^1.0.3",
    "react-scripts": "^5.0.1"
  },
  "scripts": {
    "start": "env-cmd -f .env  react-app-rewired start",
    "build": "react-scripts build"
  }
}
